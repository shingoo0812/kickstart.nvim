# ============================================================================
# Neovim Docker Environment Variables
# ============================================================================
# This file contains environment variables for docker-compose
# Copy this file to .env and modify the values as needed
# ============================================================================

# ----------------------------------------------------------------------------
# Workspace Configuration
# ----------------------------------------------------------------------------
# Path to your projects directory
# This will be mounted to /workspace inside the container
# 
# For WSL2 (Linux path):
WORKSPACE_PATH=/home/shingo/projects
#
# For Windows (if running Docker Desktop with Windows containers):
# WORKSPACE_PATH=C:/Users/shingo/projects

# ----------------------------------------------------------------------------
# Optional: Git Configuration
# ----------------------------------------------------------------------------
# Uncomment to mount your git config (useful for commits inside container)
# GIT_CONFIG_PATH=${HOME}/.gitconfig
# SSH_PATH=${HOME}/.ssh

# ----------------------------------------------------------------------------
# Optional: Custom Environment Variables
# ----------------------------------------------------------------------------
# Add any custom environment variables your projects might need
# CUSTOM_VAR=value
# API_KEY=your_api_key_here

# ----------------------------------------------------------------------------
# Notes:
# ----------------------------------------------------------------------------
# - Changes to this file require rebuilding: docker-compose build
# - Use Linux path format even on Windows when using WSL2
# - Paths with spaces should be quoted: "/path/with spaces"
# ============================================================================


